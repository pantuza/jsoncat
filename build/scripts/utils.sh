#!/bin/bash


ECHO=$(which echo);
ECHOOPTS="-en";

CAT=$(which cat);

GREP=$(which grep);

CUT=$(which cut);

SED=$(which sed);
SEDOPTS="-i";

GIT=$(which git);


# Files
VERSION_FILE="../src/messages.h";
